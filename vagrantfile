Vagrant.configure("2") do |config|

    # 2008 R2
    config.vm.box = "opentable/win-2008r2-standard-amd64-nocm"
    config.vm.provider :virtualbox do |vm|
        vm.name = "windows-audit-test"
    end
    
end